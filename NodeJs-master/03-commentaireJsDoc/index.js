/** 
 * Ceci fait une addition ! 
 * @param {number} a
 * @param {number} b
 * @returns {number}
*/
function test(){
    return a + b;
}
// en utilisant cette méthode, cela permet d'afficher la suite après un . (ex: le test(3,2) proposera les attribut spécifique aux numbers vu qu'on lui a retourné un number) 
// site pour avoir rour @ = https://jsdoc.app
test(3,2)
